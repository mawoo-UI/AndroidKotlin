문자열 사이에 값을넣음


main이 없다면
클래스기반으로해야함


var은 정말 변수처럼사용하는것
val은 불변으로 데이터값을 변경할수없음



------------------------------------------

React 컴포넌트의 주요 특징이 아닌것은?
반드시상태를 포함해야한다X
- State (재사용을 위해 사용)

컴포넌트의 렌더링을 중단시키는 메서드(라이프사이클)

컴포넌트 간에 데이터 공유위해 사용(state)
-불변성을 유지,데이터 변화를 ui에 반영

useMemo
-불필요한 계산을 방지하기 위해 값의 캐싱을 지원

useCallback
-컴포넌트의 모든 상태를 리렌더링

상태관리에서 불변성을 유지해야하는이유
-React가 변경된 상태를 감지할 수 있도록

useEffect훅에서 의존성 배열을 빈 배열로 정의했을때 동작규칙
-컴포넌트가 마운트 될 때만 실행

폼 이벤트중 onChange 이벤트에서 값을 가져오는 속성
-e.target.value

fetch API가 반환하는 타입
-Promise

async/await 키워드 주요 목적
-비동기/순차

Context API에서 useContext훅의 역할
-컨슈머의 대체 훅(상태관리)

JWT에서 발급시간을 나타내기위해 사용하는키?
-iat issued At

